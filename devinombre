<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Jeux gratuits - Devin des Nombres</title>
		<link rel="icon" href="1.png">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Devin des Nombres</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin-top: 50px; }
        input { padding: 10px; margin-top: 10px; }
        #message { margin-top: 20px; }
    </style>
</head>
<body>
    <h1>Devinez un nombre entre 1 et 100</h1>
    <input type="number" id="guess" placeholder="Entrez un nombre">
    <button onclick="checkGuess()">Essayer</button>
    <div id="message"></div>

    <script>
        const randomNumber = Math.floor(Math.random() * 100) + 1;
        let attempts = 0;

        function checkGuess() {
            const userGuess = parseInt(document.getElementById('guess').value);
            attempts++;
            const message = document.getElementById('message');

            if (userGuess === randomNumber) {
                message.textContent = `Bravo! Vous avez deviné le nombre ${randomNumber} en ${attempts} essais.`;
            } else if (userGuess < randomNumber) {
                message.textContent = 'Votre nombre est trop faible.';
            } else {
                message.textContent = 'Votre nombre est trop important.';
            }
        }
    </script>
</body>
</html>
